box: wercker/default@0.0.2
build:
  steps:
    - script:
       name: Run HelloWorld
       code: bash hello.sh
deploy:
  steps:
    - renlinx007/github-push:
        version: 0.0.1
        token: $GITHUB_TOKEN
        uname: 'sideffect0'
        email: 'mail3renjith@gmail.com'
        repo: 'nubicz/nubicz.github.io'
        branch: 'master'
        basedir: 'public'
